<div class="container twelve columns">
  <div class="row">
    <div class="eight columns offset-by-one">
      
          <%= form_for(@route, :builder => RouteFormBuilder, :html => {:class => "custom"}) do |f| %>
            <fieldset  id="add-route">
              <legend><%= "#{I18n.t(:new)} #{Route.model_name.human}" %></legend>
              <div class="row">
                  
                  <%= f.text_field :name, :class => "radius six columns bottom", :placeholder=>"#{Route.human_attribute_name(:name)}" %>
                <%= f.transport_type_field %>
              </div>
              <div class="row">
                  <%= f.text_field :origin, :class => "radius six columns", :placeholder=>"#{Route.human_attribute_name(:origin)}" %>
                  <%= f.text_field :destiny, :class => "radius six columns", :placeholder=>"#{Route.human_attribute_name(:destiny)}" %>
              </div>
              <div class="row">
                  <%= f.text_field :day, :class => "radius four columns", :placeholder=>"#{Route.human_attribute_name(:day)}" %>
                  <%= f.text_field :hour, :class => "radius four columns", :placeholder=>"#{Route.human_attribute_name(:hour)}" %>
                <%= f.repeat_in_field %>
              </div>
              <div class="row">
                <div class="four columns offset-by-eight">
                  <%= f.submit :class=>"radius button twelve", :value=>"Adicionar" %>
                </div>
              </div>
            </fieldset>
          <% end %>

    </div>
  </div>
</div>
