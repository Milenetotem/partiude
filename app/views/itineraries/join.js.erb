var form = $("#location_point").parent();
<% if @poll.errors.present? %>
  $(form).find("div.alert").remove();
  $(form).prepend("<%= j render(:partial => 'shared/form_alerts', :locals => { :entity => @poll }) %>");
<% else %>
  $("#join-modal").trigger("reveal:close");
<% end %>
