<%= form_for(@codigo) do |f| %>
  <% if @codigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@codigo.errors.count, "error") %> prohibited this codigo from being saved:</h2>

      <ul>
      <% @codigo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :empresa_id %><br />
    <%= f.number_field :empresa_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
