<div class="contain-to-grid ">
  <nav class="top-bar">
      <section>
        <%= render :partial => 'layouts/itineraries_links' if current_user.present? %>

        <ul class="right">
          <% if user_signed_in? %>
    <li class="divider"></li>
    <li>
        <%= link_to(I18n.t("devise.sessions.sign_out".to_sym), destroy_user_session_path, :class => "active", :method => :delete) %>
    </li>
<% end %>
          <li class="divider"></li>
          <li class="has-dropdown">
            <a href="/">Partiu.de</a>
            <ul class="dropdown">
              <li><a href="#"><%= I18n.t("partiude.who_we_are".to_sym) %></a></li>
              <li><a href="#"><%= I18n.t("partiude.plans".to_sym) %></a></li>
              <li><a href="#"><%= I18n.t("partiude.partners".to_sym) %></a></li>
              <li><a href="#"><%= I18n.t("partiude.contact".to_sym) %></a></li>
              <li class="divider"></li>
            </ul>
          </li>
        </ul>
      </section>
  </nav>
</div>
